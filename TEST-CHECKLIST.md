# รายการตรวจรับเวอร์ชัน 2.5.4

## ตรวจในชุดไฟล์แล้ว

- Source TS/TSX 29 ไฟล์ผ่าน syntax check
- `package.json` และ `package-lock.json` สอดคล้องกันสำหรับ `npm ci`
- ไม่มี dependency `lucide-react` ซ้ำ: ปุ่มและไอคอนฟังก์ชันใช้ Streamline ภายในโปรเจกต์
- ไม่บรรจุ `node_modules` ใน ZIP
- Desktop Header มีเมนู Checklist และ LINE โดยตรง
- Mobile Bottom Navigation + เมนูเพิ่มเติมยังทำงานเหมือนเดิม
- หน้า Checklist รองรับ สร้าง / ดู / แก้ไข / เพิ่มข้อ / ลบข้อ / เรียงข้อ / ปิดใช้งาน / ลบแม่แบบ
- System Template แก้รายการตรวจได้ แต่ล็อกรหัส ประเภท แผนก สถานะ และป้องกันการลบ เพื่อไม่ให้โครงงานเวรและการอ้างอิงแม่แบบพัง
- แม่แบบที่สร้างเองลบถาวรได้เมื่อไม่มี Work Order / PM Plan / Duty Schedule อ้างอิง
- RPC `mt_update_checklist_template` และ `mt_delete_checklist_template` เป็น SECURITY INVOKER, anon/PUBLIC เรียกไม่ได้
- ทดสอบแก้ไขและลบแม่แบบด้วย transaction + rollback บน MPRLine แล้วผ่าน
- รูปช่างตรวจชนิด JPG/PNG/WebP และจำกัด 5 MB ทั้งหน้าเว็บและ Storage bucket
- ช่างที่มีบัญชีหรือประวัติงานถูกป้องกันการลบถาวร
- Session Refresh ใช้คำขอเดียวเมื่อหน้าเว็บโหลดข้อมูลพร้อมกัน


## Responsive / Modal Acceptance Test

1. Desktop 1366×768: เปิด `เวร 5S > สร้างเวรเอง` ต้องเห็น Header และปุ่ม X ตลอดเวลา
2. รายชื่อช่างต้องมี scroll ภายใน และบนจอกว้างแสดง 2 คอลัมน์
3. เลื่อนเนื้อหา Modal ลงสุด ต้องเห็น Footer `ยกเลิก / สร้างเวรและใบงาน` โดยไม่หลุดจอ
4. กด `Esc` ต้องปิด Modal ได้
5. คลิกพื้นที่มืดนอก Manual Duty Modal ต้องปิดได้เมื่อไม่ได้กำลังบันทึก
6. ขณะ Modal เปิด หน้าเว็บด้านหลังต้องไม่เลื่อน
7. ทดสอบความสูงหน้าจอ 600–768px และ Browser Zoom 80%, 100%, 125% โดย Header/Footer ต้องยังเข้าถึงได้
8. ทดสอบ Create Task, Task Detail, PM Plan, Checklist Editor, Technician Editor และ Team Editor ว่าเนื้อหา scroll ภายในและปุ่มปิดไม่หลุด viewport
9. มือถือแนวตั้ง: เมนู `เพิ่มเติม` ต้องไม่สูงเกินจอและเลื่อนภายในได้

## ต้องให้ GitHub Actions ยืนยันก่อน Production Deploy

หลัง Push ให้ดู Action `Deploy Maintenance Pro to GitHub Pages` ต้องผ่านทั้งหมด:

1. `Install`
2. `Test and build` (`npm run typecheck` + `npm run build`)
3. `Deploy`

เครื่องตรวจชุดนี้ไม่มี npm registry cache ครบสำหรับ Vite จึงไม่อ้างว่า production build ผ่านจนกว่า GitHub Actions จะขึ้นสีเขียว

## ทดสอบกับโปรเจกต์จริง

1. Login ด้วย Admin หรือ Supervisor
2. บนคอมพิวเตอร์ตรวจว่ามี `Checklist` และ `LINE` บนแถบเมนูด้านบน
3. เปิด Checklist > เลือก `PM-IVF-MONTHLY` > กดแก้ไข
4. เปลี่ยนชื่อหัวข้อหนึ่งข้อ > เพิ่มหัวข้อใหม่ > ลบหัวข้อที่เพิ่ม > บันทึก
5. Refresh หน้า แล้วข้อมูลที่แก้ต้องยังอยู่
6. ลองแก้ System Template `DUTY-MVR` ต้องแก้ชื่อ/รายการได้ แต่ไม่มีปุ่มลบ
7. สร้างแม่แบบทดสอบใหม่ แล้วลบแม่แบบนั้นก่อนนำไปใช้ ระบบต้องลบได้
8. ถ้าแม่แบบถูกใช้กับใบงาน/PM/เวรแล้ว ปุ่มลบต้องถูกปิด และสามารถปิดใช้งานแทนได้
9. เปิดมือถือ > เพิ่มเติม > แม่แบบ Checklist ต้องเข้าหน้าเดียวกับคอมพิวเตอร์
10. สร้างงานโดยเลือกแม่แบบที่แก้แล้ว ตรวจว่าใบงานใหม่ snapshot รายการล่าสุด
11. ทดสอบ Checklist ผิดปกติ > แนบรูปในหัวข้อนั้น > Submit
12. ตรวจ LINE ว่ายังส่งอัตโนมัติตามเวลาเดิม

> โปรเจกต์ MPRLine ที่เชื่อมอยู่ได้รับ RPC Checklist Management แล้ว ไม่ต้องรัน SQL เพิ่มก่อนทดสอบเวอร์ชันนี้


## v2.5.4 Logo + LINE Flex Acceptance Test

1. เปิดหน้า Login ต้องเห็นโลโก้เฟือง/ประแจ/มือ ไม่ใช่ช่องว่างหรือไอคอนเดิม
2. Login แล้วตรวจ Header บนคอมและ Mobile Menu ว่าใช้โลโก้เดียวกัน
3. ตรวจ Favicon ของแท็บ Browser ว่าแสดงโลโก้
4. เปิดเมนู `LINE` แล้วต้องเห็นส่วน `Flex Message + Deep Link` และตัวอย่างการ์ด
5. กด `ทดสอบส่ง` ต้องได้รับ Flex Message ที่กลุ่ม LINE พร้อมโลโก้และปุ่มเปิดระบบ
6. สร้างใบงานจริง 1 งานที่อนุญาตให้ทดสอบ แล้วรอ/ประมวลผลคิว LINE: การ์ดต้องมีเลขใบงาน ชื่องาน แผนก ผู้รับผิดชอบ และปุ่ม `เปิดทำใบงาน`
7. กด `เปิดทำใบงาน` ขณะ Login อยู่ ต้องเปิด Task Detail ของใบงานนั้นทันที
8. Logout แล้วกดปุ่มจาก LINE อีกครั้ง: ระบบต้องให้ Login และกลับมาเปิดใบงานเดิมหลัง Login
9. ทดสอบปุ่มจาก LINE for Windows/macOS และมือถือ
10. ตรวจว่าเวรยังไม่ถูกสร้างเอง: หน้าเวรต้องมีเฉพาะรายการที่หัวหน้าสร้างด้วยตนเอง
