# เปิดใช้งาน “จัดการช่าง” และรูปประจำตัว

โปรเจกต์ MPRLine ที่เชื่อมกับชุดส่งมอบนี้ติดตั้งเรียบร้อยแล้ว ขั้นตอนต่อไปใช้เฉพาะเมื่อนำเว็บไปเชื่อมกับ Supabase โปรเจกต์อื่น

## 1. รัน SQL

1. เข้า Supabase Dashboard ของโปรเจกต์
2. เปิด **SQL Editor**
3. กด **New query**
4. เปิดไฟล์ `supabase/sql/202608260001_technician_admin_and_photos.sql`
5. คัดลอกทั้งหมดไปวาง แล้วกด **Run**

ผลลัพธ์ท้ายคำสั่งควรเห็น:

- ฟังก์ชัน 3 รายการ: `mt_admin_list_technicians`, `mt_admin_save_technician`, `mt_admin_delete_technician`
- Bucket ชื่อ `mt-technician-photos`
- ค่า `public` ของ Bucket เป็น `false`

## 2. รีเฟรชเว็บ

1. ออกจากระบบแล้วเข้าใหม่ด้วยบัญชี Admin
2. เปิดเมนู **ตั้งค่าระบบ**
3. เลือกแท็บ **จัดการช่าง**

Admin จะสามารถเพิ่ม แก้ไข ย้ายแผนก เปลี่ยนทีม A/B/O ปิดใช้งาน ลบ และเปลี่ยนรูปช่างได้

## กติกาการลบ

- ถ้ายังไม่มีใบงาน แผน PM หรือบัญชี Login: ลบถาวรได้
- ถ้ามีประวัติแล้ว: ระบบจะไม่ลบ ให้ใช้ **ปิดใช้งาน** แทน
- การปิดใช้งานจะซ่อนช่างจากตัวเลือกงานใหม่ แต่ชื่อยังแสดงในรายงานย้อนหลัง

## รูปช่าง

- รองรับ JPG, PNG และ WebP
- ขนาดสูงสุด 5 MB
- Bucket เป็น Private
- ผู้ใช้งานที่ Login และยัง Active ดูรูปได้
- เฉพาะ Admin เพิ่ม เปลี่ยน หรือลบรูปได้

## หมายเหตุเรื่องรหัสพนักงาน

หลังช่าง Login ครั้งแรก ระบบจะล็อกรหัสพนักงานเพื่อไม่ให้บัญชี Auth เสีย สามารถแก้ชื่อ แผนก ทีม ตำแหน่ง รูป และสถานะได้ตามปกติ
